return {
    "tpope/vim-fugitive",
    config = function()
        -- Configure vim-fugitive to open in tabs instead of splits
        -- local keymap = vim.keymap.set
        -- keymap('n', '<leader>gs', ':Git status<CR>', { desc = 'Git status in new tab' })
        -- keymap('n', '<leader>go', ':Git<CR>', { desc = 'Git status in new tab' })
        -- keymap('n', '<leader>ga', ':Git add .<CR>', { desc = 'Git status in new tab' })
        -- keymap('n', '<leader>gc', ':Git commit<CR>', { desc = 'Git commit in new tab' })
        -- keymap('n', '<leader>gd', ':Gdiff<CR>', { desc = 'Git diff in new tab' })
        -- keymap('n', '<leader>gl', ':Git log<CR>', { desc = 'Git log in new tab' })
        -- keymap('n', '<leader>gb', ':Git blame<CR>', { desc = 'Git blame in new tab' })
    end
}
