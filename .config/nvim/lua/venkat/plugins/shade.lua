return {
    -- "sunjon/shade.nvim",
    -- config = function()
    --     require("shade").setup({
    --         overlay_opacity = 50, -- Adjust opacity (0-100)
    --         opacity_step = 1,
    --         keys = {
    --             toggle = "<leader>sh", -- Keybinding to toggle dimming
    --         },
    --     })
    -- end,
}
