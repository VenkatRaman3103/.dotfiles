return {
    -- "jubnzv/virtual-types.nvim",
    -- config = function()
    --     -- This plugin doesn't have a setup function
    --     -- It should be configured in your LSP setup instead
    -- end,
}
